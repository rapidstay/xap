server:
  port: 8081

spring:
  application:
    name: rapidstay-xap

  data:
    redis:
      host: red-d40ub0k9c44c73cdgu60
      port: 6379
      timeout: 5000
  cache:
    type: redis
    redis:
      time-to-live: 1800000
      cache-null-values: false

  jackson:
    serialization:
      indent_output: true
      fail-on-empty-beans: false

logging:
  level:
    root: INFO
    com.rapidstay.xap: DEBUG

# 🧭 CORS (테스트용 — Firebase)
cors:
  allowed-origins: "https://rapidstay-c7f8e.web.app"
  allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
  allowed-headers: "*"

# 📡 Expedia API
expedia:
  api:
    url: "https://api.expediapartnersolutions.com/xap/lodging/listings"
    key: "YOUR_API_KEY_HERE"

# 🧪 Mock 모드
rapidstay:
  mock:
    enabled: true
